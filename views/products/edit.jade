.page-header
  h1 #{product.title} 
    small#{ __i('Modify this product')}

.row-fluid
  .span6
    != partial('form', { locals: { product: product, action: '/products/' + product._id + '/edit/' } }); 
